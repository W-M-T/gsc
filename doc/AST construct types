SPL
    list of VarDecl, FunDecl, TypeSyns

VarDecl
    optional type, identifier, expression

FunDecl
    kind (infix op, prefix op or function), identifier, list of identifiers, optional FunType, list of VarDecl, list of Stmt

TypeSyns
    type identifier, type

Type
    real type (type_identifier or TupleType or ListType)

FunType
	list of Type, Type

TupleType
    Type, Type

ListType
    Type

Stmt
    IfElse or
    Loop (TBD) or
    ActStmt or
    Return

IfElse
    list of (Expr, Stmt*) (last one can have None Expr)

Loop
    optional ActStmt, optional Expr, optional ActStmt, list of Stmt

ActStmt
    Assignment or FunCall

Return
    Expr

Assignment
    VarRef, Expr

FunCall
    identifier, list of Expr

Expr
	DeferredExpr or ParsedExpr

DeferredExpr
    list of terms and ops

ParsedExpr
    Literal
    VarRef
    Funcall on list of ParsedExpr

VarRef
	identifier, list of Field

